# History

This directory keeps project memory that changes over time.

Use:

- `decisions/` for architecture decision records.
- `progress/` for commit-by-commit implementation records, current state, changelog, and milestones.
- `references/` for external links, images, documents, examples, API notes, research, and design inspiration.

Do not store secrets or generated runtime data here.
