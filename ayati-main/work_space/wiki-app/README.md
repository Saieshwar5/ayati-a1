# Local Wiki App

Run locally:

1. Start: node server.js
2. Open http://localhost:3000

Pages are stored as markdown files in wiki-app/data/wiki/. The server creates a sample welcome.md on first start.
