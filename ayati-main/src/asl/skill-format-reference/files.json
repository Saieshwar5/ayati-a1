{
  "schemaVersion": "ayati-skill-file-map/v1",
  "required": [
    "skill.md",
    "skill.json",
    "tools.json",
    "guardrails.json",
    "credentials.json",
    "prerequisites.json",
    "tests.json"
  ],
  "optional": [],
  "notes": [
    "All JSON files must be UTF-8 and valid JSON (no comments).",
    "Tool names must be namespaced with skill id.",
    "Guardrails merge policy should be restrictive.",
    "tool-access update operations are declared inside guardrails.json under toolAccessPlan.",
    "prerequisites.json and tests.json are declarative during development and should not block loading."
  ]
}
