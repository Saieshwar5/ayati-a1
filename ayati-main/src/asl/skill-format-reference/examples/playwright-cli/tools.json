{
  "schemaVersion": "ayati-tools/v1",
  "skillId": "playwright-cli",
  "tools": [
    {
      "name": "playwright-cli.exec",
      "description": "Execute a single safe playwright-cli subcommand.",
      "requiresCredentials": [
        "PLAYWRIGHT_CLI_SESSION"
      ],
      "selectionHints": {
        "tags": [
          "browser",
          "playwright",
          "automation"
        ],
        "aliases": [
          "playwright_run",
          "browser_command"
        ],
        "examples": [
          "open browser",
          "goto a url",
          "take page snapshot"
        ],
        "domain": "execution",
        "priority": 25
      },
      "inputSchema": {
        "type": "object",
        "required": [
          "action"
        ],
        "properties": {
          "action": {
            "type": "string",
            "description": "Playwright CLI action, for example open or goto."
          },
          "args": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Arguments for the action."
          },
          "session": {
            "type": "string",
            "description": "Optional named session, maps to -s=<session>."
          }
        }
      },
      "executor": {
        "type": "shell_command",
        "command": "playwright-cli",
        "commandFallbacks": [
          {
            "command": "npx",
            "fixedArgs": [
              "playwright-cli"
            ]
          }
        ],
        "argsTemplate": [
          "{{#if session}}-s={{session}}{{/if}}",
          "{{action}}",
          "{{...args}}"
        ],
        "timeoutMs": 45000,
        "maxOutputChars": 80000,
        "allowedActions": [
          "open",
          "goto",
          "click",
          "dblclick",
          "fill",
          "type",
          "press",
          "drag",
          "hover",
          "select",
          "upload",
          "check",
          "uncheck",
          "snapshot",
          "screenshot",
          "tab-list",
          "tab-new",
          "tab-close",
          "tab-select",
          "go-back",
          "go-forward",
          "reload",
          "close",
          "close-all",
          "list"
        ]
      }
    }
  ]
}
