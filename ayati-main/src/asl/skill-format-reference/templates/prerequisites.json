{
  "schemaVersion": "ayati-prerequisites/v1",
  "skillId": "your-skill-id",
  "lastUpdated": "2026-02-15",
  "mode": "declarative",
  "items": [
    {
      "id": "cli-binary",
      "type": "binary",
      "required": true,
      "description": "Primary CLI binary is available in PATH.",
      "howToCheck": {
        "kind": "command",
        "command": "which your-cli"
      },
      "expected": "Exit code 0 and resolved binary path.",
      "howToFix": "Install your-cli and ensure PATH contains the install location."
    },
    {
      "id": "fallback-runner",
      "type": "node_package",
      "required": false,
      "description": "npx is available for command fallback execution.",
      "howToCheck": {
        "kind": "command",
        "command": "npx --yes your-cli --help"
      },
      "expected": "Help command returns successfully.",
      "howToFix": "Install Node.js and npm, then retry."
    },
    {
      "id": "workspace-write",
      "type": "filesystem",
      "required": false,
      "description": "Skill can write artifacts to an approved workspace or temp directory.",
      "howToCheck": {
        "kind": "manual",
        "instructions": "Confirm output directory exists and is writable."
      },
      "expected": "Directory write operations succeed.",
      "howToFix": "Use /tmp or update permissions for the target directory."
    }
  ]
}
